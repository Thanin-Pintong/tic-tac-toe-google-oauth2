<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="/stylesheets/bootstrap.min.css" rel="stylesheet" />
  <link href="/stylesheets/style.css" rel="stylesheet" />
  <script src="/javascripts/bootstrap.bundle.min.js"></script>
  <title>{{#if viewData.title}}{{ viewData.title }}{{else}}Online Test{{/if}}</title>
</head>
<body>
  <!-- header -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-secondary py-4">
    <div class="container">
      <a class="navbar-brand" href="#">Online Test {{#if session.user}}({{ session.user.email }}){{/if}}</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup"
        aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <div class="navbar-nav ms-auto">
          <!--
          <a class="nav-link active" href="/">Home</a>
          <a class="nav-link active" href="#">About</a>
          -->
          {{#if session.user}}          
          <!--<div class="vr bg-white mx-2 d-none d-lg-block"></div>-->
          <a class="nav-link active" href="/auth/logout">Logout</a>
          {{/if}}
        </div>
      </div>
    </div>
  </nav>

  <header class="masthead bg-primary text-white text-center py-4">
    <div class="container d-flex align-items-center flex-column">
      <h2>Tic-Tac-Toe</h2>
    </div>
  </header>
  <!-- header -->

  <div class="container my-4">
    {{> content}}
  </div>

  <!-- footer -->
  <div class="copyright py-4 text-center text-white">
    <div class="container">
      <small>
        Copyright - Thanin Pintong
      </small>
    </div>
  </div>
  <!-- footer -->
</body>
</html>